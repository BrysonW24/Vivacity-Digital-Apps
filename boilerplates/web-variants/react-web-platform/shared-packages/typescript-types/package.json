{
  "name": "@lib/typescript-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types and interfaces",
  "type": "module",
  "main": "dist/index.d.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prebuild": "rimraf dist",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "format": "prettier --write \"src/**/*.{ts,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.2.2",
    "prettier": "^3.0.3",
    "rimraf": "^5.0.5"
  }
}
